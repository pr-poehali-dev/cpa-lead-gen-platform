# CPAsibo Pro - CPA Платформа

## 🚀 Что создано

Полнофункциональная CPA платформа для генерации лидов с автоматическим отслеживанием через пиксель.

## 📊 База данных

Создана PostgreSQL база со следующими таблицами:
- **users** - пользователи (рекламодатели, вебмастера, админы)
- **offers** - офферы рекламодателей
- **clicks** - отслеживание кликов
- **conversions** - конверсии и выплаты
- **sessions** - сессии авторизации

## 🔌 Backend API

### 1. Авторизация (`/backend/auth`)
**URL**: https://functions.poehali.dev/1b266b18-98f0-4d9b-bbed-9be7df6878ad

**Регистрация:**
```json
POST /
{
  "action": "register",
  "email": "user@example.com",
  "password": "password123",
  "role": "webmaster" // или "advertiser"
}
```

**Вход:**
```json
POST /
{
  "action": "login",
  "email": "user@example.com",
  "password": "password123"
}
```

**Проверка токена:**
```json
POST /
{
  "action": "verify",
  "token": "your-token-here"
}
```

### 2. Управление офферами (`/backend/offers`)
**URL**: https://functions.poehali.dev/199a463e-7a51-4b45-be71-87abf0d87353

**Получить список офферов:**
```
GET /?status=active
```

**Создать оффер:**
```json
POST /
{
  "name": "Название оффера",
  "description": "Описание",
  "payout": 1500,
  "category": "Категория",
  "advertiser_id": 1
}
```

### 3. Отслеживание пикселя (`/backend/pixel`)
**URL**: https://functions.poehali.dev/cc9da18f-4fd0-4bf0-9764-17e7eea9fb9c

**Получить скрипт пикселя:**
```
GET /?action=pixel
```

**Код для размещения на сайте рекламодателя:**
```html
<script src="https://functions.poehali.dev/cc9da18f-4fd0-4bf0-9764-17e7eea9fb9c?action=pixel" data-offer-id="1"></script>
```

**На странице "Спасибо за заявку" добавить:**
```html
<script>
  window.cpasibo.trackConversion();
</script>
```

### 4. Статистика (`/backend/stats`)
**URL**: https://functions.poehali.dev/2707476c-e24a-4166-85cf-fa57e6456526

**Получить статистику:**
```
GET /?user_id=1&role=webmaster&period=30
```

Роли: `webmaster`, `advertiser`, `admin`

## 👥 Тестовые пользователи

Все пароли: `password`

| Email | Роль | Описание |
|-------|------|----------|
| webmaster@test.com | Вебмастер | Баланс: 25,000₽ |
| advertiser@test.com | Рекламодатель | Есть активные офферы |
| admin@cpasibo.pro | Админ | Полный доступ |

## 📝 Бизнес-логика

### Для рекламодателя:
1. Регистрация на платформе
2. Создание оффера (минимальная ставка 500₽)
3. Получение кода пикселя
4. Размещение пикселя на странице "спасибо"
5. Отправка тестового лида
6. Оплата предоплаты (x20 от ставки)
7. Активация оффера

### Для вебмастера:
1. Регистрация на платформе
2. Выбор оффера из каталога
3. Получение партнерской ссылки:
   ```
   сайтрекламодателя.ру/?utm_source=cpasibo_pro&utm_medium=cpl&wm_id=YOUR_ID
   ```
4. Трафик → Клики отслеживаются автоматически
5. Конверсии → Баланс пополняется автоматически

### Комиссии:
- Платформа берёт 20% с каждого лида
- Вебмастер получает 80% от ставки
- Рекламодатель платит 100% ставки + 20% комиссии

## 🔐 Безопасность

- Пароли хешируются (SHA-256)
- Токены сессий действуют 30 дней
- CORS настроен для всех origins
- Все API возвращают JSON с кодами состояния

## 📈 Статистика включает:

- Общее количество кликов
- Количество конверсий
- Заработок/Расход
- Активные офферы
- График по дням
- Топ вебмастеров (для админа)
- Детализация по офферам
